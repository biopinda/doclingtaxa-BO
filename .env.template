# MongoDB Configuration
# Production MongoDB credentials (update with your actual server details)
# Format: mongodb://username:password@host:port/?authSource=admin
MONGODB_URI=mongodb://dwc2json:VLWQ8Bke65L52hfBM635@192.168.1.10:27017/?authSource=admin
MONGODB_DATABASE=dwc2json
MONGODB_COLLECTION=monografias

# MongoDB Test Configuration (for running tests)
# Use local MongoDB instance for testing to avoid production data modification
MONGODB_TEST_URI=mongodb://localhost:27017

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Application Configuration
# Directory for PDF processing (default: ./monografias)
# PDF_INPUT_DIR=./monografias

# Processing Options
# MAX_WORKERS=1  # Number of parallel PDF processors (1 = sequential)
# PROCESSING_TIMEOUT=300  # Timeout per PDF in seconds (0 = no timeout)
